
const initState = {
  
};

function CatagoryReducer(state = initState, action) {
    switch (action.type) {
        case :

        default:
            state;
    }
}

export default CatagoryReducer;